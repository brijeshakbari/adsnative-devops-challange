description "AdsNative"

start on (filesystem)
stop on runlevel [016]

respawn
setuid nobody
setgid nogroup
chdir {{ repository_path }}

exec gunicorn app:app --bind unix:/tmp/adsnative.sock --workers 3
#exec gunicorn app:app --bind unix:/tmp/adsnative.sock --workers 3 >> /var/log/adsnative/app.log 2>&1 
