description "AdsNative"

start on (filesystem)
stop on runlevel [016]

respawn
setuid nobody
setgid nogroup
chdir /webapps/adsnative

exec gunicorn app:app --bind unix:/tmp/adsnative.sock >> /var/log/adsnative/app.log 2>&1 
